import wollok.game.*
import config.*
import menu.*

program juego {
	game.title("Turbo Barman")
	game.cellSize(10)
	game.height(50)
	game.width(100)
	game.boardGround('turboAlgoLey2.png')
	menuPrincipal.iniciar()
	configSonido.cargarAudio()
	game.start()
}